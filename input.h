#ifndef INPUT_H
#define INPUT_H

using namespace std;

#include <vector>
#include "driver.h"
#include "rider.h"

void readDrivers(vector<driver>& drivers);
void readRiders(vector<rider>& riders);

#endif
